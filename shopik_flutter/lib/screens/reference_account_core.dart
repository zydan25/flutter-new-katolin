import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../core/app_controller.dart';
import '../widgets/common.dart';
import 'screen_common.dart';
import 'reference_security.dart';

class OperationsView extends StatefulWidget { const OperationsView({super.key}); @override State<OperationsView> createState()=>_OperationsViewState(); }
class _OperationsViewState extends State<OperationsView>{final search=TextEditingController();String filter='all';@override void dispose(){search.dispose();super.dispose();}@override Widget build(BuildContext context){final app=context.watch<AppController>();final q=search.text.trim().toLowerCase();final rows=app.operations.where((o){final s='${o['status']??''}'.toLowerCase();final text='${o['id']??''} ${o['phone']??''} ${o['service']??o['packageName']??''}'.toLowerCase();final match=filter=='all'||(filter=='success'&&(s=='success'||s=='completed'))||(filter=='pending'&&(s=='pending'||s=='queued'||s=='processing'))||(filter=='failed'&&(s=='failed'||s=='rejected'));return(q.isEmpty||text.contains(q))&&match;}).toList();return ScreenFrame(title:'سجل العمليات',color:AppColors.burgundy,actions:[IconButton(onPressed:app.refreshWalletAndReports,icon:const Icon(Icons.refresh_rounded))],child:Column(children:[Container(width:double.infinity,color:const Color(0xFFFEF3C7),padding:const EdgeInsets.all(9),child:const Text('البيانات معروضة من خادم Django فقط.',style:TextStyle(fontSize:9.5,color:Color(0xFF92400E),fontWeight:FontWeight.w800))),Container(color:Colors.white,padding:const EdgeInsets.all(10),child:Column(children:[TextField(controller:search,onChanged:(_)=>setState((){}),decoration:const InputDecoration(prefixIcon:Icon(Icons.search_rounded),hintText:'بحث برقم العملية أو الهاتف أو الخدمة...',filled:true,fillColor:Color(0xFFF1F5F9),border:InputBorder.none,isDense:true)),const SizedBox(height:6),SingleChildScrollView(scrollDirection:Axis.horizontal,child:Row(children:[for(final e in const {'all':'الكل','success':'ناجحة','pending':'قيد الانتظار','failed':'فاشلة'}.entries)Padding(padding:const EdgeInsets.only(right:5),child:ChoiceChip(selected:filter==e.key,label:Text(e.value,style:const TextStyle(fontSize:9,fontWeight:FontWeight.w900)),selectedColor:AppColors.burgundy,labelStyle:TextStyle(color:filter==e.key?Colors.white:const Color(0xFF0F172A)),onSelected:(_)=>setState(()=>filter=e.key)))]))])),Expanded(child:RefreshIndicator(onRefresh:app.refreshWalletAndReports,child:ListView(padding:const EdgeInsets.all(12),children:[if(rows.isEmpty)const EmptyState(text:'لا توجد عمليات مطابقة.',icon:Icons.receipt_long_outlined),for(final row in rows)RefOperationTile(operation:row,onTap:()=>showDialog(context:context,builder:(_)=>OperationDetailModal(operation:row)))])))]));}}

class OperationDetailModal extends StatelessWidget{const OperationDetailModal({super.key,required this.operation});final Map<String,dynamic> operation;@override Widget build(BuildContext context){final s='${operation['status']??''}'.toLowerCase();final ok=s=='success'||s=='completed';final data={'رقم العملية':'${operation['id']??'-'}','الخدمة':'${operation['service']??operation['packageName']??'-'}','الهاتف':'${operation['phone']??'-'}','المبلغ':'${operation['amount']??0} ${operation['currency']??'YER'}','الحالة':'${operation['status']??'-'}','التاريخ':'${operation['created_at']??operation['date']??'-'}'};return AlertDialog(shape:RoundedRectangleBorder(borderRadius:BorderRadius.circular(20)),title:Row(children:[Icon(ok?Icons.check_circle_rounded:Icons.info_outline_rounded,color:ok?AppColors.emerald:AppColors.amber),const SizedBox(width:6),const Text('تفاصيل العملية',style:TextStyle(fontWeight:FontWeight.w900))]),content:Column(mainAxisSize:MainAxisSize.min,children:[for(final e in data.entries)Padding(padding:const EdgeInsets.symmetric(vertical:4),child:Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[Text(e.key,style:const TextStyle(fontSize:9,color:AppColors.muted)),Flexible(child:Text(e.value,textAlign:TextAlign.left,style:const TextStyle(fontSize:10,fontWeight:FontWeight.w900)))])]),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('إغلاق'))]));}}

class AccountStatementScreen extends StatelessWidget{const AccountStatementScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'كشف الحساب',color:AppColors.teal,actions:[IconButton(onPressed:app.refreshWalletAndReports,icon:const Icon(Icons.refresh_rounded))],child:RefreshIndicator(onRefresh:app.refreshWalletAndReports,child:ListView(padding:const EdgeInsets.all(12),children:[PageCard(child:Column(children:[const Text('الرصيد الحالي',style:TextStyle(fontSize:9,color:AppColors.muted)),Text(money(app.walletBalance),style:const TextStyle(fontSize:28,color:AppColors.burgundy,fontWeight:FontWeight.w900)),Text('${app.statement.length} قيداً محاسبياً',style:const TextStyle(fontSize:9,color:AppColors.muted))])),const SizedBox(height:10),if(app.statement.isEmpty)const EmptyState(text:'لا توجد قيود محاسبية.',icon:Icons.account_balance_wallet_outlined),for(final row in app.statement)PageCard(margin:const EdgeInsets.only(bottom:7),child:ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.swap_vert_rounded,color:AppColors.blue),title:Text('${row['description']??row['service']??row['reference']??'قيد محاسبي'}',style:const TextStyle(fontSize:10,fontWeight:FontWeight.w900)),subtitle:Text('${row['date']??row['created_at']??''}',style:const TextStyle(fontSize:8,color:AppColors.muted)),trailing:Text('${row['amount']??row['value']??0} ${row['currency']??'YER'}',style:const TextStyle(fontSize:9,fontWeight:FontWeight.w900)))]])));}}

class ReportsScreen extends StatelessWidget{const ReportsScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();final success=app.operations.where((o){final s='${o['status']??''}';return s=='success'||s=='completed';}).length;final failed=app.operations.where((o){final s='${o['status']??''}';return s=='failed'||s=='rejected';}).length;return ScreenFrame(title:'التقارير والإحصائيات',color:AppColors.indigo,child:RefreshIndicator(onRefresh:app.refreshWalletAndReports,child:ListView(padding:const EdgeInsets.all(12),children:[Row(children:[Expanded(child:RefMetric(title:'كل العمليات',value:'${app.operations.length}',icon:Icons.receipt_long_rounded,color:AppColors.blue)),const SizedBox(width:6),Expanded(child:RefMetric(title:'ناجحة',value:'$success',icon:Icons.check_circle_rounded,color:AppColors.emerald)),const SizedBox(width:6),Expanded(child:RefMetric(title:'فاشلة',value:'$failed',icon:Icons.error_outline_rounded,color:Colors.red))]),const SizedBox(height:10),PageCard(child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[const Text('ملخص مباشر من الخادم',style:TextStyle(fontSize:12,fontWeight:FontWeight.w900)),const SizedBox(height:6),Text('الرصيد: ${money(app.walletBalance)}',style:const TextStyle(fontSize:10.5,fontWeight:FontWeight.w900,color:AppColors.burgundy)),Text('المنتجات: ${app.products.length} • المتاجر: ${app.vendors.length} • الطلبات: ${app.orders.length}',style:const TextStyle(fontSize:9,color:AppColors.muted))])),const SizedBox(height:10),const RefSection(title:'آخر العمليات الناجحة',icon:Icons.trending_up_rounded,color:AppColors.emerald),const SizedBox(height:7),for(final row in app.operations.where((o){final s='${o['status']??''}';return s=='success'||s=='completed';}).take(8))RefOperationTile(operation:row)])));}}

class SubscriberTransferScreen extends StatefulWidget{const SubscriberTransferScreen({super.key});@override State<SubscriberTransferScreen>createState()=>_SubscriberTransferScreenState();}
class _SubscriberTransferScreenState extends State<SubscriberTransferScreen>{final receiver=TextEditingController();final amount=TextEditingController();final note=TextEditingController();Map<String,dynamic>? lookup;bool busy=false;@override void dispose(){receiver.dispose();amount.dispose();note.dispose();super.dispose();}Future<void>_lookup()async{if(receiver.text.trim().isEmpty)return;setState(()=>busy=true);try{lookup=await context.read<AppController>().recipientLookup(receiver.text.trim());}catch(e){if(mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}finally{if(mounted)setState(()=>busy=false);}}Future<void>_send()async{final value=double.tryParse(amount.text.trim());if(value==null||value<=0||lookup==null)return;setState(()=>busy=true);try{final r=await context.read<AppController>().api.transfer(recipient:receiver.text.trim(),amount:value,note:note.text.trim());await context.read<AppController>().refreshWalletAndReports();if(mounted)await showDialog<void>(context:context,builder:(_)=>AlertDialog(title:const Text('نجح التحويل',textAlign:TextAlign.center),content:Text('المبلغ: ${r['amount']??value} ر.ي\nالمستلم: ${lookup!['receiver_name']??receiver.text}\nالقيد: ${r['journal']??r['id']??'-'}',textAlign:TextAlign.center),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('تم'))]));}catch(e){if(mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}finally{if(mounted)setState(()=>busy=false);}}@override Widget build(BuildContext context)=>ScreenFrame(title:'تحويل لمشترك',color:AppColors.amber,child:ListView(padding:const EdgeInsets.all(12),children:[PageCard(child:Column(children:[TextField(controller:receiver,textDirection:TextDirection.ltr,keyboardType:TextInputType.phone,decoration:const InputDecoration(labelText:'رقم المستلم',prefixIcon:Icon(Icons.person_search_rounded))),const SizedBox(height:8),SizedBox(width:double.infinity,child:OutlinedButton.icon(onPressed:busy?null:_lookup,icon:const Icon(Icons.search_rounded),label:const Text('فحص المستلم')))])),if(lookup!=null)...[const SizedBox(height:8),PageCard(child:ListTile(contentPadding:EdgeInsets.zero,leading:const CircleAvatar(backgroundColor:Color(0xFFFFFBEB),child:Icon(Icons.person,color:AppColors.amber)),title:Text('${lookup!['receiver_name']??receiver.text}',style:const TextStyle(fontSize:11,fontWeight:FontWeight.w900)),subtitle:Text('${lookup!['receiver_phone']??receiver.text}',style:const TextStyle(fontSize:9,color:AppColors.muted))))],const SizedBox(height:8),PageCard(child:Column(children:[TextField(controller:amount,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'المبلغ',suffixText:'ر.ي')),const SizedBox(height:8),TextField(controller:note,maxLines:2,decoration:const InputDecoration(labelText:'ملاحظة اختيارية')),const SizedBox(height:12),SizedBox(width:double.infinity,child:FilledButton.icon(onPressed:busy||lookup==null?null:_send,style:FilledButton.styleFrom(backgroundColor:AppColors.amber,foregroundColor:Colors.black87),icon:const Icon(Icons.send_rounded),label:const Text('تنفيذ التحويل',style:TextStyle(fontWeight:FontWeight.w900))))]))]));}}

class WifiNetworksScreen extends StatelessWidget{const WifiNetworksScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'شبكات وكروت الوايفاي',color:AppColors.teal,actions:[IconButton(onPressed:app.refreshAll,icon:const Icon(Icons.refresh_rounded))],child:RefreshIndicator(onRefresh:app.refreshAll,child:ListView(padding:const EdgeInsets.all(12),children:[const RefSection(title:'الشبكات المتاحة',icon:Icons.wifi_rounded,color:AppColors.teal),const SizedBox(height:7),if(app.wifi.isEmpty)const EmptyState(text:'لا توجد شبكات متاحة من الخادم.',icon:Icons.wifi_off_outlined),for(final n in app.wifi)PageCard(margin:const EdgeInsets.only(bottom:8),child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[Text('${n['name']??'شبكة'}',style:const TextStyle(fontSize:13,fontWeight:FontWeight.w900)),Text('${n['location']??n['description']??''}',style:const TextStyle(fontSize:9,color:AppColors.muted)),if(n['denominations'] is List)for(final raw in (n['denominations'] as List).whereType<Map>())ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.confirmation_number_outlined,color:AppColors.teal),title:Text('${raw['name']??'فئة'}',style:const TextStyle(fontSize:10,fontWeight:FontWeight.w800)),subtitle:Text('${raw['available_cards']??''} متاح',style:const TextStyle(fontSize:8)),trailing:TextButton(onPressed:()=>_buy(context,n,Map<String,dynamic>.from(raw)),child:Text('${raw['sale_price']??raw['price']??''} ر.ي',style:const TextStyle(fontSize:10,fontWeight:FontWeight.w900))))])),const SizedBox(height:8),const RefSection(title:'الكروت المشتراة',icon:Icons.confirmation_number_outlined,color:AppColors.blue),const SizedBox(height:7),if(app.wifiCards.isEmpty)const EmptyState(text:'لا توجد كروت مشتراة.',icon:Icons.confirmation_number_outlined),for(final c in app.wifiCards)PageCard(margin:const EdgeInsets.only(bottom:7),child:Text('PIN: ${c['pin_code']??'-'}\nSerial: ${c['serial_number']??'-'}\nالشبكة: ${c['network_name']??'-'}',style:const TextStyle(fontSize:10,fontWeight:FontWeight.w800))) ])));}
Future<void>_buy(BuildContext context,Map n,Map<String,dynamic> d)async{final networkId=int.tryParse('${n['id']??''}'),denomId=int.tryParse('${d['id']??''}'),price=double.tryParse('${d['sale_price']??d['price']??0}');if(networkId==null||denomId==null||price==null)return;try{final app=context.read<AppController>();final r=await app.api.wifiPurchase(networkId:networkId,denominationId:denomId,phone:app.user?.phone??'',price:price);await app.refreshAll();if(context.mounted)await showDialog<void>(context:context,builder:(_)=>AlertDialog(title:const Text('تم شراء الكرت'),content:Text('PIN: ${r['pin']??r['pin_code']??'-'}\nSerial: ${r['card_number']??r['serial_number']??'-'}'),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('تم'))]));}catch(e){if(context.mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}}
}

class UserProfileEditScreen extends StatelessWidget{const UserProfileEditScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'الملف الشخصي',color:const Color(0xFF475569),child:ListView(padding:const EdgeInsets.all(12),children:[PageCard(child:Column(children:[ListTile(leading:const CircleAvatar(backgroundColor:AppColors.burgundy,child:Icon(Icons.person,color:Colors.white)),title:Text(app.user?.name??'',style:const TextStyle(fontWeight:FontWeight.w900)),subtitle:Text(app.user?.phone??'')),ListTile(leading:const Icon(Icons.location_on_outlined),title:const Text('المحافظة'),trailing:Text(app.user?.governorate??'')),const Divider(),const Text('بيانات الحساب معروضة مباشرة من خادم Django. تعديل الملف سيستخدم عقد الخادم عند توفره.',style:TextStyle(fontSize:9.5,color:AppColors.muted))]))]));}}

class SettingsScreen extends StatelessWidget{const SettingsScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'الإعدادات',color:const Color(0xFF475569),child:ListView(padding:const EdgeInsets.all(12),children:[PageCard(child:Column(children:[ListTile(contentPadding:EdgeInsets.zero,leading:const CircleAvatar(backgroundColor:AppColors.burgundy,child:Icon(Icons.person,color:Colors.white)),title:Text(app.user?.name??'',style:const TextStyle(fontWeight:FontWeight.w900)),subtitle:Text(app.user?.phone??'')),ListTile(leading:const Icon(Icons.account_balance_wallet_outlined),title:const Text('الرصيد'),trailing:Text(money(app.walletBalance),style:const TextStyle(fontWeight:FontWeight.w900,color:AppColors.burgundy))),ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const UserProfileEditScreen())),leading:const Icon(Icons.person_outline),title:const Text('الملف الشخصي')),ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const FingerprintSettingsScreen())),leading:const Icon(Icons.fingerprint_rounded),title:const Text('البصمة والأمان')),ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const NotificationsScreen())),leading:const Icon(Icons.notifications_none_rounded),title:const Text('الإشعارات')),ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const AccountStatementScreen())),leading:const Icon(Icons.receipt_long_outlined),title:const Text('كشف الحساب')),ListTile(onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const SupportScreen())),leading:const Icon(Icons.support_agent_rounded),title:const Text('التواصل مع الإدارة')),const Divider(),ListTile(onTap:app.refreshAll,leading:const Icon(Icons.sync_rounded),title:const Text('مزامنة البيانات')),ListTile(onTap:()=>showAboutDialog(context:context,applicationName:'شبيك | SHOPIK',applicationVersion:'1.0.0',applicationLegalese:'Yemen Code for Smart Technologies'),leading:const Icon(Icons.info_outline_rounded),title:const Text('عن التطبيق')),ListTile(onTap:app.logout,leading:const Icon(Icons.logout_rounded,color:Colors.red),title:const Text('تسجيل الخروج',style:TextStyle(color:Colors.red,fontWeight:FontWeight.w800))]))]));}}

class NotificationsScreen extends StatelessWidget{const NotificationsScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'الإشعارات',color:const Color(0xFF475569),child:RefreshIndicator(onRefresh:app.refreshAll,child:ListView(padding:const EdgeInsets.all(12),children:[if(app.notifications.isEmpty)const EmptyState(text:'لا توجد إشعارات من الخادم.',icon:Icons.notifications_none_rounded),for(final n in app.notifications)PageCard(margin:const EdgeInsets.only(bottom:7),child:ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.notifications_active_outlined,color:AppColors.burgundy),title:Text('${n['title']??'إشعار'}',style:const TextStyle(fontSize:11,fontWeight:FontWeight.w900)),subtitle:Text('${n['body']??n['message']??''}',style:const TextStyle(fontSize:10)),trailing:TextButton(onPressed:int.tryParse('${n['id']??''}')==null?null:()async{await app.api.markNotificationRead(int.parse('${n['id']}'));await app.refreshAll();},child:const Text('قراءة',style:TextStyle(fontSize:9))))])));}}

class SupportScreen extends StatefulWidget{const SupportScreen({super.key});@override State<SupportScreen>createState()=>_SupportScreenState();}
class _SupportScreenState extends State<SupportScreen>{final input=TextEditingController();Map<String,dynamic>? data;bool busy=false;@override void initState(){super.initState();_load();}@override void dispose(){input.dispose();super.dispose();}Future<void>_load()async{try{data=await context.read<AppController>().api.support();}catch(_){ }if(mounted)setState((){});}Future<void>_send()async{if(input.text.trim().isEmpty)return;setState(()=>busy=true);try{data=await context.read<AppController>().api.sendSupportMessage(input.text.trim());input.clear();}catch(e){if(mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}finally{if(mounted)setState(()=>busy=false);}}@override Widget build(BuildContext context){final raw=data?['conversation'];final messages=raw is Map&&raw['messages'] is List?(raw['messages'] as List).whereType<Map>().toList():<Map>[];return ScreenFrame(title:'التواصل مع الإدارة',color:const Color(0xFF475569),child:Column(children:[Expanded(child:messages.isEmpty?const Center(child:Text('لا توجد رسائل بعد.',style:TextStyle(color:AppColors.muted))):ListView(padding:const EdgeInsets.all(12),children:[for(final m in messages){Align(alignment:m['sender_role']=='customer'?Alignment.centerRight:Alignment.centerLeft,child:Container(margin:const EdgeInsets.only(bottom:7),padding:const EdgeInsets.all(10),decoration:BoxDecoration(color:m['sender_role']=='customer'?AppColors.burgundy:Colors.white,borderRadius:BorderRadius.circular(14),border:Border.all(color:AppColors.border)),child:Text('${m['body']??''}',style:TextStyle(fontSize:10,color:m['sender_role']=='customer'?Colors.white:const Color(0xFF0F172A))))}])) ,SafeArea(child:Container(color:Colors.white,padding:const EdgeInsets.all(9),child:Row(children:[Expanded(child:TextField(controller:input,maxLines:2,decoration:const InputDecoration(hintText:'اكتب رسالتك...',isDense:true))),IconButton(onPressed:busy?null:_send,style:IconButton.styleFrom(backgroundColor:AppColors.burgundy,foregroundColor:Colors.white),icon:const Icon(Icons.send_rounded))])))]));}}

class AddressesScreen extends StatelessWidget{const AddressesScreen({super.key});@override Widget build(BuildContext context){final app=context.watch<AppController>();return ScreenFrame(title:'عناوين التوصيل',color:AppColors.blue,child:ListView(padding:const EdgeInsets.all(12),children:[if(app.addresses.isEmpty)const EmptyState(text:'لا توجد عناوين محفوظة.',icon:Icons.location_off_outlined),for(final a in app.addresses)PageCard(margin:const EdgeInsets.only(bottom:7),child:ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.location_on_rounded,color:AppColors.blue),title:Text('${a['label']??a['name']??'عنوان'}',style:const TextStyle(fontSize:11,fontWeight:FontWeight.w900)),subtitle:Text('${a['address']??a['details']??a['city']??''}',style:const TextStyle(fontSize:9,color:AppColors.muted)),trailing:IconButton(onPressed:()=>_delete(context,a),icon:const Icon(Icons.delete_outline_rounded,color:Colors.red)))),const SizedBox(height:6),FilledButton.icon(onPressed:()=>_add(context),style:FilledButton.styleFrom(backgroundColor:AppColors.blue),icon:const Icon(Icons.add_location_alt_outlined),label:const Text('إضافة عنوان'))]));}
Future<void>_delete(BuildContext context,Map a)async{final id=int.tryParse('${a['id']??''}');if(id==null)return;try{await context.read<AppController>().api.deleteAddress(id);await context.read<AppController>().refreshAll();}catch(e){if(context.mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}}
Future<void>_add(BuildContext context)async{final label=TextEditingController(),address=TextEditingController(),city=TextEditingController();await showDialog<void>(context:context,builder:(_)=>AlertDialog(title:const Text('إضافة عنوان'),content:Column(mainAxisSize:MainAxisSize.min,children:[TextField(controller:label,decoration:const InputDecoration(labelText:'اسم العنوان')),TextField(controller:address,decoration:const InputDecoration(labelText:'العنوان')),TextField(controller:city,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'city_id'))]),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('إلغاء')),FilledButton(onPressed:()async{try{await context.read<AppController>().api.createAddress({'label':label.text.trim(),'address':address.text.trim(),'city_id':int.tryParse(city.text.trim())});await context.read<AppController>().refreshAll();if(context.mounted)Navigator.pop(context);}catch(e){if(context.mounted)ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString())));}},child:const Text('حفظ'))]));label.dispose();address.dispose();city.dispose();}}
