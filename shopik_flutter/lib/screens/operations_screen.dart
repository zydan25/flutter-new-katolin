import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../core/app_controller.dart';
import '../widgets/common.dart';

class OperationsScreen extends StatelessWidget { const OperationsScreen({super.key}); @override Widget build(BuildContext context){final app=context.watch<AppController>(); return Scaffold(appBar:AppBar(title:const Text('سجل العمليات')),body:RefreshIndicator(onRefresh:app.refreshWalletAndReports,child:ListView(padding:const EdgeInsets.all(12),children:[PageCard(child:Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[const Text('العمليات من الخادم',style:TextStyle(fontWeight:FontWeight.w900)),Text('${app.operations.length}',style:const TextStyle(fontWeight:FontWeight.w900,color:AppColors.blue))])),const SizedBox(height:10),if(app.operations.isEmpty)const PageCard(child:Text('لا توجد عمليات مسترجعة حاليًا.',textAlign:TextAlign.center)) else ...app.operations.map((o)=>Padding(padding:const EdgeInsets.only(bottom:8),child:PageCard(child:Column(children:[Row(children:[const Icon(Icons.receipt_long,color:AppColors.blue),const SizedBox(width:8),Expanded(child:Text('${o['service']??'خدمة'}',style:const TextStyle(fontSize:12,fontWeight:FontWeight.w900))),Text('${o['status']??''}',style:TextStyle(fontSize:10,fontWeight:FontWeight.w900,color:o['status']=='success'?Colors.green:Colors.orange))]),const Divider(),Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[Text('${o['amount']??''} ${o['currency']??'YER'}',style:const TextStyle(fontWeight:FontWeight.w900)),Text('${o['created_at']??''}',style:const TextStyle(fontSize:10,color:Colors.black54))]),if(o['error_message']!=null)Align(alignment:Alignment.centerRight,child:Text('${o['error_message']}',style:const TextStyle(color:Colors.red,fontSize:10))) ]))))])));}
}
